import { ComparePanel } from "../features/compare/ComparePanel";

/** Compare 2-6 securities over the same window with the same method. */
export function ComparePage() {
  return (
    <div className="page page--single">
      <ComparePanel />
    </div>
  );
}
